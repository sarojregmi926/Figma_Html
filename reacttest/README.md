When the current schema is set to "test" using USE test;:

You can simply use books to refer to the "books" table.

When working within the "test" schema:

Use test.books to perform operations specifically on the "books" table within the "test" schema.

